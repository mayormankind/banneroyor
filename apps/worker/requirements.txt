# Pinned-compatible runtime deps (mirror pyproject.toml). Install with:
#   pip install -r requirements.txt
openai>=1.99.0,<2
supabase>=2.10.0,<3
pydantic>=2.9,<3
pydantic-settings>=2.5,<3
httpx>=0.27,<1
